---
name: add-sudden
description: 한글 문장들을 지정한 파일에 추가한다. 각 문장마다 한글/일본어 섹션 쌍을 만들고, 일본어는 비워둔다.
argument-hint: <file_path> <한글 문장1> / <한글 문장2> / ...
user-invocable: true
---

# 한글 문장 추가

## 동작 흐름

1. `$ARGUMENTS`에서 파일 경로와 한글 문장들을 파싱한다.
2. 파일 경로가 없으면 오늘 날짜의 `sudden/YYYY-MM-DD.md`를 기본으로 사용한다.
3. 각 한글 문장마다 `## 한글` + `## 일본어` 섹션 쌍을 파일 끝에 추가한다.
4. 일본어 섹션은 `-` (빈 항목)으로 남긴다.

## 인자 파싱 규칙

- 문장 구분자는 `/`이다.
- 첫 번째 인자가 `.md`로 끝나는 경로이면 파일 경로로 인식한다.
- 파일 경로가 없으면 `sudden/YYYY-MM-DD.md`를 사용한다.

예시:
- `/add-sudden sudden/2026-03-16.md 오늘 점심 든든하네 / 태양이 5개입니다`
- `/add-sudden 오늘 점심 든든하네 / 태양이 5개입니다` → `sudden/YYYY-MM-DD.md`에 추가

## 출력 형식

파일 끝에 각 문장마다 아래 형식을 추가한다:

```markdown
## 한글

- 한글 문장

## 일본어

-
```

## 규칙

1. 대상 파일이 존재하지 않으면 안내 후 중단한다.
2. 문장이 하나도 없으면 안내 후 중단한다.
3. 각 문장의 앞뒤 공백을 제거한다.
4. 파일 끝에 빈 줄이 없으면 빈 줄을 추가한 뒤 섹션을 붙인다.
5. 완료 후 추가한 문장 수와 파일 경로를 알려준다.
